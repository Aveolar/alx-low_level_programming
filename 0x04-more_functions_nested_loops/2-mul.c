#include "main.h"

/**
 * mul - write a function that multiplies two integers.
 *
 * @a: mul int parameter
 * @b: mul int parameter
 * Return: Always 0 (Success)
 */
int mul(int a, int b)
{
	return (a * b);
}
